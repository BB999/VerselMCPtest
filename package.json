{
  "name": "vercel-mcp-hello",
  "version": "1.0.0",
  "description": "Vercel MCP server that returns hello greetings",
  "main": "api/server.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "node api/server.js",
    "test": "node scripts/test-client.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "type": "module",
  "engines": {
    "node": "20"
  }
}